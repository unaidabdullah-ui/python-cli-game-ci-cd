[tool.black]
line-length = 100
target-version = ["py312"]

[tool.isort]
profile = "black"
line_length = 100

[tool.pytest.ini_options]
addopts = "--cov=game --cov-report=term-missing --cov-report=xml"
testpaths = ["."]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
